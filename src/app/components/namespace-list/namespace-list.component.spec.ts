import { NamespaceListComponent } from './namespace-list.component';

describe('NamespaceListComponent', () => {
  let component: NamespaceListComponent;

  beforeEach(() => {
    component = new NamespaceListComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
